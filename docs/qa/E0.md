# QA — E0 Setup (2026-09-09)

## Feito
- `git init` em `urban-canaa/` (sem commit ainda; commits só a pedido do usuário). `.gitignore` bloqueia dados brutos, `*.csv/*.txt/*.dbf/*.tif/*.zip`, `.venv`, `node_modules`, `web/dist`.
- Ambiente: `uv venv --python 3.13` + `uv pip install -r pyproject.toml` (pandas 3.0.5, geopandas 1.1.4, rasterio 1.5.1 / GDAL 3.12.4, duckdb, pyarrow, scikit-learn, statsmodels, linearmodels, rioxarray, xarray, pystac-client, planetary-computer, odc-stac, stackstac, dask, scikit-image, topojson, mapclassify, contextily, dbfread, pdfplumber, pytest). Instalação editável do projeto falhou (descoberta de pacotes do setuptools) — irrelevante; usa-se `-r pyproject.toml`.
- Reuso: `pipeline/lib/` copiado de `migracoes-mineracao/scripts/lib` (commit 67dde19); `pipeline/{disclosure_rules,disclosure_check,verify_gate}.py` de `atlas-migração/pipeline` (commit 1c05d27) — **adaptar em E2**; layouts 2000/2010 (JSON) em `data/interim/layouts/`; `artigo/docx_pipeline/` (md_to_docx.js, ardosia_docx.js); Ardósia: `web/src/styles/ardosia.css`, `web/public/marks/*.svg`, `pipeline/lib/ardosia_mpl.py` (= `palette.py` da skill); `artigo/bibliografia/declaracao_ia_padrao.md`.
- Symlinks: `data/raw/microdados_local` → pasta local de microdados; `data/raw/zeitmaschine` → disco externo de backup (ambos fora do repositório).
- `CLAUDE.md` (regras de sigilo, caminhos, regime de execução, convenções), `README.md`, `PLANO.md` (plano aprovado + checklist).
- `pipeline/validate.py --smoke`: 6 blocos (pacotes, caminhos, layouts, libs, APIs IBGE/INPE/Planetary Computer, ferramentas) — **todos OK**.

## Verificações
- Caminhos relativos das libs copiadas resolvem para a raiz deste projeto (`Path(__file__).parent.parent.parent`): `fwf.LAYOUTS_DIR`, `deflator.CACHE`, `malhas.BASE`, `mineracao.BASE`; `disclosure_check.ROOT`/`verify_gate.ROOT` = raiz.
- Layout 2010 pessoas contém as variáveis adicionais necessárias (V0002, V0011, V1006, V0010, V0626, V6264, V6400, V6527).

## Pendências para E1
- Nada bloqueante. `disclosure_rules.py` ainda tem os limiares do atlas (5/3, arredondamento 5, dimensões do atlas) — trocar em E2 pelos limiares do plano (20/10 em 2022; 10 em 2000/2010/1991; arredondamento 10/50).
- `npm install` em `artigo/docx_pipeline/` concluído (pacote `docx` presente).
