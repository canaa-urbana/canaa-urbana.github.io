# QA — E8 Publicação e QA final (2026-09-10)

Etapa E8 do checklist do `PLANO.md`: verify_gate, checklist Ardósia, preparação do deploy no
GitHub Pages, README/Metodologia, declaração de IA. Sessão principal Opus 5 (o regime previa
Fable 5.1 para a E8; o usuário pediu para seguir nesta sessão). Sem subagentes. Pedidos do
usuário durante a etapa: seguir o **modelo de publicação do atlas-migração, com Zenodo e DOI**, e
garantir que **nenhum dado pessoal, chave de API ou informação sigilosa** seja publicado.

## Decisões do titular (10/09/2026)

- Organização própria no GitHub: **`canaa-urbana`**, repositório `canaa-urbana/canaa-urbana.github.io`,
  site em `https://canaa-urbana.github.io`.
- Autoria nas citações: **Sobreira, Daniel Pessini** (ORCID 0000-0002-6632-3991).
- Licenças: **MIT** (código) + **CC BY 4.0** (dados, figuras, textos, artigo), com as exceções de
  terceiros (OpenStreetMap ODbL; Copernicus; USGS) em `LICENSE-DADOS.md`.
- Commits locais autorizados, com o e-mail no-reply do GitHub; **push só com nova confirmação**.

## Entregáveis

| Arquivo | Conteúdo |
|---|---|
| `LICENSE`, `LICENSE-DADOS.md` | MIT (código); CC BY 4.0 + tabela de exceções de terceiros e aviso sobre os microdados |
| `CITATION.cff`, `.zenodo.json` | metadados de citação e do depósito no Zenodo (DOI a propagar depois da release) |
| `.github/workflows/publicar.yml` | verificar (gate `--clone`, auditoria de dados pessoais, gitleaks no histórico com checksum do binário, histórico sem bruto/CSV) → construir (npm ci, build, `.nojekyll`, 404, checagem pós-build) → publicar (Pages) |
| `.github/dependabot.yml`, `requirements-ci.txt` | atualizações semanais (npm, actions); dependências mínimas do CI |
| `.gitleaks.toml` | regras padrão + exceção só para os SHA-256 do gate e do manifesto do painel |
| `.githooks/pre-commit` | `verify_gate.py` + `auditoria_publicacao.py --staged` (ativo via `core.hooksPath`) |
| `pipeline/auditoria_publicacao.py` | varre o conteúdo **decodificado** de todo arquivo versionado (texto, parquet, DOCX, PDF, metadados de imagem): e-mail, CPF, telefone, caminho local, URL assinada, chaves conhecidas, arquivos proibidos |
| `pipeline/verify_gate.py --clone` | aceita a ausência dos tipos ignorados pelo git (CSV, TIF) num clone |
| `docs/METODOLOGIA.md`, `docs/CHECKLIST_PUBLICACAO.md`, `README.md` | metodologia consolidada; checklist do titular (modelo do atlas); README completo com achados, arquitetura, reprodução, licenças, como citar, aviso, IA, resumo em inglês |
| painel | `web/src/lib/publicacao.ts` (URL, repositório, DOI — ponto único de atualização), bloco "Como citar" nas abas Artigo e Metodologia, rodapé, `index.html` com canônica, Open Graph e JSON-LD (`Dataset`), `robots.txt`, `sitemap.xml` |
| artigo | seção "Disponibilidade de dados e código" (site, repositório, Zenodo; DOI a atribuir); DOCX/PDF regenerados |

## Auditoria de sigilo e de dados pessoais

Varredura dos 625 arquivos versionados (bytes e conteúdo decodificado). Encontrado e corrigido:

1. **4 e-mails de terceiros** nos resumos de repositórios acadêmicos em
   `data/processed/bibliografia/candidatos.parquet` → substituídos por "[e-mail removido]"; a
   remoção entrou no `30_bibliografia.py` (`sem_dados_pessoais`), para ser reprodutível. Como
   `data/processed` mudou, o gate foi recarimbado (`2026-09-10-e8`).
2. **Nº de protocolo da consulta ao IBGE** e **localização do termo de compromisso** no `PLANO.md`
   → removidos.
3. **Caminhos do disco local** (`/Volumes/…`, `~/Documents/…`) em `CLAUDE.md`, `PLANO.md` e QA da
   E0/E2 → descrições genéricas.
4. **Nome da pasta de entrega dos microdados de 2022** (traz data e hora da entrega controlada) em
   `12_microdados.py` e `validate.py` → busca por padrão; documentação com `<entrega>`.
5. **Tamanhos amostrais**: "~700 pessoas amostrais" da área rural de 2022 (a célula que a R7
   protege) no QA da E2 e contagens amostrais exatas de Parauapebas 1991 no cabeçalho do
   `12_microdados.py` → redigidos; totais ponderados não oficiais da tabela de validação da E2
   arredondados a 10 (R2).
6. `PLANO.docx` e `PLANO.pdf` (cópias com o conteúdo antigo) e `.claude/` ficam **fora** do
   repositório (`.gitignore`).

Sem ocorrência: chave de API, token, URL assinada (as consultas STAC não guardam tokens nos
dados), CPF, e-mail pessoal (os scripts usam `urban-canaa@example.org` no "polite pool" das APIs),
nome de usuário nos metadados de DOCX/PDF ("Un-named") e de PNG. O nome do link simbólico local
`data/raw/zeitmaschine` continua no código (não é dado pessoal e não aparece em dado publicado).

## Verificações

- `validate.py`: `--smoke`, `--e2`, `--e3a`, `--e3b`, `--e3c`, `--e4`, `--e5`, `--e6`, `--e7` — **todos OK**.
- Gate: `disclosure_check.py --versao 2026-09-10-e8` **aprovado** (R1–R8, 20.458 células
  recontadas, 5.951 rurais implícitas, 259 arquivos); `verify_gate.py` e `verify_gate.py --clone`
  **aprovados**.
- `auditoria_publicacao.py`: **0 ocorrências** em todos os arquivos versionados.
- `gitleaks` 8.30.1 (binário oficial, checksum SHA-256 conferido) sobre o histórico: ver abaixo.
- Histórico do git: nenhum arquivo de `data/raw`, `data/interim`, CSV, TXT (exceto
  `requirements-ci.txt` e `robots.txt`) ou DBF.
- Build de produção: sem sourcemaps, sem caminho local, com `robots.txt`, `sitemap.xml`, PDF e
  manifesto do painel; tamanho do repositório ~111 MB (maior arquivo 19 MB, abaixo do limite de
  100 MB do GitHub).
- Checklist Ardósia (painel e artigo): `validate --e5` (hex dos SVG) e `--e7` (hex de `web/src`)
  OK, com a exceção documentada das cores oficiais de uso do solo; contraste AA verificado com
  axe-core na E7; monograma no artigo, glifo de coorte no painel.

## Pendências (dependem do titular)

1. **Decidir sobre a consulta ao IBGE** (cobertura da divulgação pública de agregados) — ver o
   primeiro item do `docs/CHECKLIST_PUBLICACAO.md`.
2. **Criar a organização `canaa-urbana` e o repositório** (recomendado: privado no primeiro push,
   conferir o CI, depois público + Pages com Source "GitHub Actions").
3. **Autorizar o push** (a sessão faz o `git push` pela chave SSH já configurada na máquina).
4. **Zenodo**: conectar o repositório no Zenodo antes da release `v1.0.0`; depois, propagar o DOI
   (lista exata no checklist).
5. MSS 1973/1982 com cenas de estação seca e corregistro manual (opcional, herdado da E7).
